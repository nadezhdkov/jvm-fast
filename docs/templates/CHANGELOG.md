# Changelog

<!-- prettier-ignore-start -->

<!--
  Layout reference, adapted from uv's CHANGELOG.md. jvm-fast has not published a release yet
  (see README.md#versionamento), so this file starts empty aside from the template below.

  Conventions:
  - One `##` section per released version, newest first.
  - Each version starts with "Released on YYYY-MM-DD." on its own line.
  - Entries are grouped under `###` subsections; use only the ones that apply to a given release.
    Suggested subsection order, adapted to jvm-fast's domain (drop unused ones per release):
      - Breaking changes
      - JDK              (JDK install/list/use, Adoptium integration, java-version resolution)
      - Resolution        (manifest parsing, version ranges, BOMs, exclusions, mediation)
      - Cache             (content-addressable cache, SQLite index)
      - Build / Run / Test (javac/java invocation, JUnit Platform Console Standalone)
      - Enhancements
      - Preview features
      - Configuration
      - Performance
      - Bug fixes
      - Documentation
  - Each entry ends with a link to the pull request that introduced it, e.g.:
      - Add `jvmfast jdk use` command ([#123](https://github.com/nadezhdkov/jvm-fast/pull/123))
  - Breaking changes get a short explanation of what changed and how to migrate, following the
    style of uv's 0.12.0 entry (see example below) — not just a one-line bullet.
-->

## Unreleased

### Breaking changes

<!-- Example of the expected depth for a breaking change entry:

- **Short title of the change** ([#0](https://github.com/nadezhdkov/jvm-fast/pull/0))

  One or two sentences of context: what the old behavior was, why it changed, and what a user
  needs to do differently, if anything.
-->

### JDK

### Resolution

### Cache

### Build / Run / Test

### Enhancements

### Preview features

### Configuration

### Performance

### Bug fixes

### Documentation

<!-- prettier-ignore-end -->
